/* 
 * This file has been automatically generated by the jrl-cmakemodules.
 * Please see https://github.com/jrl-umi3218/jrl-cmakemodules/blob/master/config.hh.cmake for details.
*/

#ifndef VIVEPLUGIN_CONFIG_HH
# define VIVEPLUGIN_CONFIG_HH

// Package version (header).
# define VIVEPLUGIN_VERSION_UNKNOWN_TAG 0 // Used to mention that the current version is unknown.
# define VIVEPLUGIN_VERSION "0.0.1"
# define VIVEPLUGIN_MAJOR_VERSION 0
# define VIVEPLUGIN_MINOR_VERSION 0
# define VIVEPLUGIN_PATCH_VERSION 1

#define VIVEPLUGIN_VERSION_AT_LEAST(major, minor, patch) (VIVEPLUGIN_MAJOR_VERSION>major || (VIVEPLUGIN_MAJOR_VERSION>=major && \
                                                             (VIVEPLUGIN_MINOR_VERSION>minor || (VIVEPLUGIN_MINOR_VERSION>=minor && \
                                                                                                     VIVEPLUGIN_PATCH_VERSION>=patch))))

#define VIVEPLUGIN_VERSION_AT_MOST(major, minor, patch) (VIVEPLUGIN_MAJOR_VERSION<major || (VIVEPLUGIN_MAJOR_VERSION<=major && \
                                                            (VIVEPLUGIN_MINOR_VERSION<minor || (VIVEPLUGIN_MINOR_VERSION<=minor && \
                                                                                                     VIVEPLUGIN_PATCH_VERSION<=patch))))

// Handle portable symbol export.
// Defining manually which symbol should be exported is required
// under Windows whether MinGW or MSVC is used.
//
// The headers then have to be able to work in two different modes:
// - dllexport when one is building the library,
// - dllimport for clients using the library.
//
// On Linux, set the visibility accordingly. If C++ symbol visibility
// is handled by the compiler, see: http://gcc.gnu.org/wiki/Visibility
# if defined _WIN32 || defined __CYGWIN__
// On Microsoft Windows, use dllimport and dllexport to tag symbols.
#  define VIVEPLUGIN_DLLIMPORT __declspec(dllimport)
#  define VIVEPLUGIN_DLLEXPORT __declspec(dllexport)
#  define VIVEPLUGIN_DLLLOCAL
# else
// On Linux, for GCC >= 4, tag symbols using GCC extension.
#  if __GNUC__ >= 4
#   define VIVEPLUGIN_DLLIMPORT __attribute__ ((visibility("default")))
#   define VIVEPLUGIN_DLLEXPORT __attribute__ ((visibility("default")))
#   define VIVEPLUGIN_DLLLOCAL  __attribute__ ((visibility("hidden")))
#  else
// Otherwise (GCC < 4 or another compiler is used), export everything.
#   define VIVEPLUGIN_DLLIMPORT
#   define VIVEPLUGIN_DLLEXPORT
#   define VIVEPLUGIN_DLLLOCAL
#  endif // __GNUC__ >= 4
# endif // defined _WIN32 || defined __CYGWIN__

# ifdef VIVEPLUGIN_STATIC
// If one is using the library statically, get rid of
// extra information.
#  define VIVEPLUGIN_DLLAPI
#  define VIVEPLUGIN_LOCAL
# else
// Depending on whether one is building or using the
// library define DLLAPI to import or export.
#  ifdef viveplugin_EXPORTS
#   define VIVEPLUGIN_DLLAPI VIVEPLUGIN_DLLEXPORT
#  else
#   define VIVEPLUGIN_DLLAPI VIVEPLUGIN_DLLIMPORT
#  endif // VIVEPLUGIN_EXPORTS
#  define VIVEPLUGIN_LOCAL VIVEPLUGIN_DLLLOCAL
# endif // VIVEPLUGIN_STATIC
#endif //! VIVEPLUGIN_CONFIG_HH
